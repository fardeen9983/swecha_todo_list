import 'package:flutter/material.dart';

class PendingToDoItem extends StatelessWidget {
  const PendingToDoItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: Text(""),
    );
  }
}
